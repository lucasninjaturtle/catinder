import React from 'react';
import { SafeAreaView, Text, StyleSheet } from 'react-native';

export const TabChatScreen = () => {
  return (
    <SafeAreaView style={styles.container}>
        <Text />
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'white',
  },
});
